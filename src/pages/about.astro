---
import BaseLayout from "@/layouts/BaseLayout.astro";
import { getEntryBySlug } from "@/lib/content";

const entry = await getEntryBySlug("pages", "en", "about");
if (!entry) {
  throw new Error('Missing "about" page content for locale "en".');
}

const { Content } = await entry.render();
---

<BaseLayout active="none" description={entry.data.summary} locale="en" path="/about/" title={`${entry.data.title} | OpenClaw101`}>
  <article class="section card page-prose" data-pagefind-body>
    <h1>{entry.data.title}</h1>
    <p class="sub">{entry.data.summary}</p>
    <Content />
  </article>
</BaseLayout>
